<nav class="navbar navbar-expand-lg navbar-light bg-light">
    <div class="container-fluid">
        <div class="collapse navbar-collapse" id="navbarColor02">
            <ul class="navbar-nav me-auto">
                <li class="nav-item">
                    <a class="nav-link active" href="{{ path('admin.index') }}">Liste utilisateurs
                        <span class="visually-hidden">(current)</span>
                    </a>
                </li>
                <li class="nav-item">
                    <a class="nav-link active" href="{{ path('Admin.new') }}">Créer un compte
                        <span class="visually-hidden">(current)</span>
                    </a>
                </li>
            </ul>

                    {% if app.user %}
                        <div>
                           <p class="mt-1"> Vous êtes connecté en tant que {{ app.user.login }}  </p> <a class="text-decoration-none btn btn-outline-danger btn-sm ms-3 " href="{{ path('app_logout') }}"> <svg xmlns="http://www.w3.org/2000/svg" width="16" height="" fill="currentColor" class="bi bi-power" viewBox="0 0 16 16">
                                    <path d="M7.5 1v7h1V1h-1z"/>
                                    <path d="M3 8.812a4.999 4.999 0 0 1 2.578-4.375l-.485-.874A6 6 0 1 0 11 3.616l-.501.865A5 5 0 1 1 3 8.812z"/>
                                </svg> Déconnexion</a>
                        </div>
                    {% endif %}
        </div>
    </div>
</nav>
